#.SUFFIXES:
.SUFFIXES: .ms .pdf

SRCS=$(shell find -type f -name '*.ms' )
TARGET := $(addsuffix .pdf,$(basename $(SRCS)))

all: clean ${TARGET}

%.pdf: %.ms
	pdfroff -b -wall -mspdf -kpte -T pdf $< > $@

lint: ${SRCS}
	writegood --parse $<
	proselint $<

clean:
	rm -f *.pdf

.PHONY: clean all lint
