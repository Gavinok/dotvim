#.SUFFIXES:
.SUFFIXES: .ms .pdf

SRCS=$(shell find -type f -name '*.ms' )
TARGET := $(addsuffix .pdf,$(basename $(SRCS)))

all: clean ${TARGET}

%.pdf: %.ms
	groff -b -wall -mspdf -pte -T pdf $< > $@

clean:
	rm *.pdf

.PHONY: clean all
