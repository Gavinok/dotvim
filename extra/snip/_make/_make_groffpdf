#.SUFFIXES:
.SUFFIXES: .ms .pdf

SRCS=$(shell find -type f -name '*.ms' )
TARGET := $(addsuffix .pdf,$(basename $(SRCS)))

all: clean ${TARGET}

%.pdf: %.ms
	groff -b -wall -mspdf -kpte -T pdf $< > $@

clean:
	rm -f *.pdf

.PHONY: clean all
